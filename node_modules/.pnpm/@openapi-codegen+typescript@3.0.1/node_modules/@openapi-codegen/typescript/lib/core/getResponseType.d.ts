import ts from "typescript";
import { ComponentsObject, ResponsesObject } from "openapi3-ts";
/**
 * Extract types from responses
 */
export declare const getResponseType: ({ responses, components, filter, printNodes, }: {
    responses: ResponsesObject;
    components?: ComponentsObject | undefined;
    filter: (statusCode: string) => boolean;
    printNodes: (nodes: ts.Node[]) => string;
}) => ts.TypeNode;
